<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.guet.mapper.editor.TravelMapper">
	<select id="getAllTravel" resultType="travel">
        SELECT * FROM travel
    </select>

    <insert id="insertTravel" parameterType="travel">
        INSERT INTO travel VALUES (#{travelid},#{travelurl},#{travelalt},#{travelhref})
    </insert>
</mapper>